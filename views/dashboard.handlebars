<section class="container ">
  <div class="row pt-0">
    <a class="text-primary" href="/"> Home</a>
    <a class="text-primary" href="#"> &nbsp;/ Dashboard</a>
      <div class="col-12 mt-2">
        <div class="accordion" id="new-post">
          <div class="accordion-item">
            <h2 class="accordion-header" id="headingOne">
              <button class="btn collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                Create New Project
              </button>
            </h2>
            <div id="collapseOne" class="accordion-collapse collapse" aria-labelledby="headingOne" data-bs-parent="#new-post">
              <div class="accordion-body">
                  <hr />
                  <form class="new-post-form">
                    <div class="col-8 form-group">
                      <label for="post-title" class="font-weight-bold">Title</label>
                      <input class="form-control" type="text" id="post-title" name="post-title" />
                    </div>
                    <div class="col-12 form-group">
                      <label for="description" class="font-weight-bold">Content2</label>
                      <input type="hidden" id="description" name="description"></input>
                      <trix-editor class="trix-content" input="description"></trix-editor>
                    </div>
                    <div class="col-3 form-group">
                      <button type="submit" class="btn">Create</button>
                    </div>
                  </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<hr />

<section class="container">
  <h2 class="mt-5 mb-5">Your Projects</h2>
  {{#if projects.length}}
  <ul>
    {{#each projects as |project|}}
    <li class="mb-5">
      
      {{> project-info project truncate='truncate' dashboard='dashboard'}}
      <a class="text-primary" href="/dashboard/edit/{{projects.id}}">Edit Project</a>
    </li>
    {{/each}}
  </ul>
  {{else}}
  <p>
    You don't have any post yet. Start now!
  </p>
  {{/if}}

</section>



<script src="/js/add-post.js"></script>