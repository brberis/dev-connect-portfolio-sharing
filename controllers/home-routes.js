

const router = require('express').Router();


// get all posts for homepage
router.get('/', (req, res) => {

  })
    .then(() => {
    
      res.render('homepage', {
     
      });
    })

module.export=router;

 
